---
phase: 02-ui-component-migration
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - components/LoginForm.tsx
  - components/RegisterForm.tsx
  - app/page.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Login form displays at localhost:3000 with JumpIn branding, email input, password input, and orange submit button"
    - "Registration form displays all required fields: Nome, Cognome, Email, Scuola dropdown, Data di Nascita, Password"
    - "Selecting 'Altro (Specifica)' from the school dropdown reveals a custom text input below it"
    - "Selecting any other school hides the custom input"
    - "Both forms use glassmorphism styling (liquid-glass card, glass-input fields, btn-primary-liquid button)"
    - "Submit buttons console.log form values (no navigation — Phase 3 wires auth)"
  artifacts:
    - path: "components/LoginForm.tsx"
      provides: "Login view with email/password fields and glassmorphism styling"
      exports: ["default LoginForm"]
    - path: "components/RegisterForm.tsx"
      provides: "Registration view with all fields and school dropdown conditional logic"
      exports: ["default RegisterForm"]
    - path: "app/page.tsx"
      provides: "Entry point that renders LoginForm"
      contains: "LoginForm"
  key_links:
    - from: "app/page.tsx"
      to: "components/LoginForm.tsx"
      via: "import and render"
      pattern: "import LoginForm"
    - from: "components/RegisterForm.tsx"
      to: "lib/schools.ts"
      via: "import RIMINI_SCHOOLS"
      pattern: "from.*lib/schools"
    - from: "components/LoginForm.tsx"
      to: "components/RegisterForm.tsx"
      via: "navigation link (onClick sets view state — Phase 2: no state yet, use console.log or TODO comment)"
      pattern: "RegisterForm|register"
---

<objective>
Extract the login and registration views from the monolithic App.tsx into standalone Next.js Client Components: LoginForm.tsx and RegisterForm.tsx.

Purpose: These components hold the visual entry points for all users. Phase 2 goal is visual fidelity — forms must look identical to the original Vite app. Auth logic (navigation, localStorage) is deferred to Phase 3.

Output: components/LoginForm.tsx, components/RegisterForm.tsx, updated app/page.tsx rendering LoginForm.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ui-component-migration/02-CONTEXT.md
@.planning/phases/02-ui-component-migration/02-RESEARCH.md
@.planning/phases/02-ui-component-migration/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and CSS classes the executor needs. Extracted from source files. -->

From lib/schools.ts (created in plan 02-01):
```typescript
export interface SchoolOption {
  value: string;
  label: string;
}

export const RIMINI_SCHOOLS: SchoolOption[] = [
  { value: 'Liceo Scientifico A. Einstein', label: 'Liceo Scientifico A. Einstein' },
  { value: 'Liceo Classico G. Cesare - M. Valgimigli', label: 'Liceo Classico G. Cesare - M. Valgimigli' },
  { value: 'ITTS O. Belluzzi - L. Da Vinci', label: 'ITTS O. Belluzzi - L. Da Vinci' },
  { value: 'Liceo Artistico Serpieri', label: 'Liceo Artistico Serpieri' },
  { value: 'Istituto Tecnico R. Valturio', label: 'Istituto Tecnico R. Valturio' },
  { value: 'IPSIA L.B. Alberti', label: 'IPSIA L.B. Alberti' },
  { value: 'ISISS P. Gobetti - A. De Gasperi (Morciano)', label: 'ISISS P. Gobetti - A. De Gasperi (Morciano)' },
  { value: 'altro', label: 'Altro (Specifica)' },
];
```

Glassmorphism CSS classes from app/globals.css:
- `.liquid-glass` — glass card background with blur, border, shadow. Apply to outer card div with `p-8 rounded-[2rem] w-full`
- `.glass-input` — frosted glass input fields. Apply with `w-full px-5 py-4 rounded-2xl placeholder:text-gray-300`
- `.btn-primary-liquid` — orange gradient button. Apply with `w-full py-5 rounded-2xl`

Lucide icons available (lucide-react@0.563.0): ChevronRight, AlertCircle, X

Animation classes from original App.tsx (verify at runtime — may need tailwindcss-animate):
- `animate-in slide-in-from-bottom-8 duration-700` — login form entrance
- `animate-in slide-in-from-right-8 duration-700` — register form entrance
- `animate-in slide-in-from-top-2 duration-300` — error banner entrance
- `animate-in slide-in-from-top-2` — "Altro" custom input appearance
If animate-in classes produce no visible effect at runtime, replace with `transition-all` for the "Altro" input, and remove entrance animations (they are enhancement-only, not structural).

Source reference — original login view (App.tsx lines 98-172):
- Branding: h1 "JumpIn" in text-5xl font-bold font-montserrat text-orange-600, subtitle "Digital Experience" in text-[10px] uppercase tracking-[0.3em]
- GlassCard wraps the form (now use liquid-glass directly instead of GlassCard component)
- Error banner: red bg-red-50/80 with AlertCircle icon, only shown when loginError is set
- "Crea un account" link at bottom navigates to register

Source reference — original register view (App.tsx lines 174-279):
- Grid for Nome/Cognome: `grid grid-cols-2 gap-4`
- School select: glass-input + appearance-none + ChevronRight icon overlay (rotate-90)
- "Altro" conditional: `{school === 'altro' && (<div className="animate-in slide-in-from-top-2">...)}`
- DOB: `<input type="date">` with glass-input
- Password: minLength={8}
- "Hai un account?" link at bottom navigates to login

Phase 2 behavior for navigation links: Add `onClick={() => console.log('navigate to register')}` placeholder. Do NOT implement actual view switching — that is Phase 3.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create components/LoginForm.tsx</name>
  <files>components/LoginForm.tsx</files>
  <action>
    Create components/ directory at project root if it does not exist, then create components/LoginForm.tsx.

    This is a Client Component (requires 'use client' — uses useState for form fields).

    Implement faithfully from App.tsx login view (lines 98-172). Key implementation details:
    - 'use client' directive at top
    - useState for email, password, loginError (string | null)
    - handleSubmit: e.preventDefault(), console.log({ email, password }) — no auth logic
    - Outer wrapper: `<div className="w-full max-w-md animate-in slide-in-from-bottom-8 duration-700">`
    - Branding block: h1 "JumpIn" + subtitle "Digital Experience" (exact text and classes from original)
    - Glass card: `<div className="liquid-glass p-8 rounded-[2rem] w-full">` (replaces GlassCard component)
    - Error banner inside card (only shown when loginError !== null): red bg-red-50/80, AlertCircle icon, text-xs
    - Form fields: Email (type="email", required) and Password (type="password", required) with glass-input styling and label above each
    - Submit button: btn-primary-liquid, text "Continua", ChevronRight icon with group-hover:translate-x-1
    - Divider + "Crea un account" link: onClick logs to console (placeholder for Phase 3)
    - TEST credentials hint below card: `demo@example.com / password123` in frosted pill (copy from original lines 164-170)

    Do NOT import from old components/GlassCard.tsx or root types.ts — LoginForm is self-contained.
  </action>
  <verify>
    Run: `cd "D:/!programming/botika/jumpInLoginAndQrCode" && npx tsc --noEmit 2>&1 | grep LoginForm`
    Expected: no output (no TypeScript errors for LoginForm.tsx).
    Also: `cat components/LoginForm.tsx | grep "'use client'"` must output the directive.
  </verify>
  <done>components/LoginForm.tsx exists, exports default LoginForm, has 'use client', useState for form fields, all original UI elements (branding, glass card, error state, two inputs, submit button, register link, test hint). TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create components/RegisterForm.tsx with school dropdown logic</name>
  <files>components/RegisterForm.tsx</files>
  <action>
    Create components/RegisterForm.tsx. This is a Client Component (requires 'use client' — uses useState for 6 form fields + conditional school input).

    Implement faithfully from App.tsx register view (lines 174-279). Key implementation details:
    - 'use client' directive at top
    - Imports: `import { RIMINI_SCHOOLS } from '@/lib/schools';` (use @/ alias)
    - useState for: firstName, lastName, email, school (default: RIMINI_SCHOOLS[0].value), customSchool, dob, password
    - handleSubmit: e.preventDefault(), console.log all field values — no auth logic
    - Outer wrapper: `<div className="w-full max-w-md animate-in slide-in-from-right-8 duration-700 py-10">`
    - Branding: h1 "Nuovo Account" + subtitle "Benvenuto in JumpIn"
    - Glass card: `<div className="liquid-glass p-8 rounded-[2rem] w-full">`
    - Form layout (inside card, space-y-4):
      * Nome + Cognome: grid grid-cols-2 gap-4, each with label + glass-input
      * Email: full-width label + glass-input
      * Scuola: label + `<div className="relative">` containing select (glass-input + appearance-none) + ChevronRight icon overlay (absolute right-4, rotate-90, pointer-events-none, text-orange-400)
      * Conditional "Altro" input: `{school === 'altro' && (<div className="animate-in slide-in-from-top-2">label + input</div>)}`
      * Data di Nascita: label + `<input type="date">` with glass-input
      * Password: label + `<input type="password" minLength={8}>` with glass-input
    - Submit button: btn-primary-liquid, full-width, text "Registrati ora"
    - "Hai un account?" link: onClick logs to console (placeholder)

    School select options: `{RIMINI_SCHOOLS.map(s => <option key={s.value} value={s.value} className="bg-white">{s.label}</option>)}`

    Do NOT import from old components/GlassCard.tsx or root types.ts.
  </action>
  <verify>
    Run: `cd "D:/!programming/botika/jumpInLoginAndQrCode" && npx tsc --noEmit 2>&1 | grep RegisterForm`
    Expected: no output (no TypeScript errors).
    Also: `cat components/RegisterForm.tsx | grep "lib/schools"` — must show the import.
    Also: `cat components/RegisterForm.tsx | grep "altro"` — must show the conditional rendering check.
  </verify>
  <done>components/RegisterForm.tsx exists, exports default RegisterForm, has 'use client', imports RIMINI_SCHOOLS from @/lib/schools, all 6 fields present, school === 'altro' conditional input shown/hidden correctly, submit console.logs values. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LoginForm into app/page.tsx</name>
  <files>app/page.tsx</files>
  <action>
    Update app/page.tsx to import and render LoginForm:

    ```tsx
    import LoginForm from '@/components/LoginForm';

    export default function Home() {
      return (
        <div className="min-h-screen flex flex-col items-center justify-center p-6 sm:p-4">
          <LoginForm />
        </div>
      );
    }
    ```

    This page itself stays a Server Component (no 'use client' needed on page.tsx — LoginForm handles its own client boundary). The centering div wrapper matches the original App.tsx root div.

    Note: app/page.tsx renders LoginForm only. RegisterForm is not rendered here in Phase 2 — it will be conditionally shown via view state managed in Phase 3.
  </action>
  <verify>
    Run: `cd "D:/!programming/botika/jumpInLoginAndQrCode" && npx next build 2>&1 | tail -10`
    Expected: Build succeeds. No errors about LoginForm or missing imports.
    Also: `cat app/page.tsx | grep "LoginForm"` — must show the import and usage.
  </verify>
  <done>app/page.tsx imports LoginForm from @/components/LoginForm and renders it inside the centering wrapper. next build succeeds. No 'use client' on page.tsx itself.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npx tsc --noEmit` completes with no errors relating to LoginForm.tsx or RegisterForm.tsx
2. `npx next build` succeeds
3. Visit localhost:3000 (next dev): JumpIn branding appears, email/password inputs render with glassmorphism, orange submit button visible
4. LoginForm's "Crea un account" link click logs to console (no navigation error)
5. RegisterForm (when rendered directly for testing): school dropdown shows 8 options, selecting "Altro (Specifica)" shows custom text input, selecting any other option hides it
</verification>

<success_criteria>
- components/LoginForm.tsx: 'use client', email + password inputs, glassmorphism card, JumpIn branding, submit console.logs, error banner state present
- components/RegisterForm.tsx: 'use client', all 6 fields (nome, cognome, email, scuola, dob, password), "Altro" conditional reveals custom input, imports RIMINI_SCHOOLS from @/lib/schools
- app/page.tsx: renders LoginForm, stays Server Component, centering wrapper matches original App.tsx root div
- `next build` succeeds — no TypeScript errors, no missing modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-component-migration/02-02-SUMMARY.md` using the summary template at `.claude/get-shit-done/templates/summary.md`.
</output>
