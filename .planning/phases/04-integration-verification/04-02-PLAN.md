---
phase: 04-integration-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-integration-verification/04-UAT.md
autonomous: false
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - INF-01
  - INF-02
  - INF-03
  - INF-04
  - INF-05

must_haves:
  truths:
    - "User can complete full flow: register → auto-login → dashboard → scan QR → timestamp appears → logout → login again"
    - "Glassmorphism styles (mesh background, glass cards, glass inputs) are visually present on all views"
    - "School dropdown Altro selection reveals custom text input"
    - "Camera shows live feed on mobile after permission granted (or Italian error message if denied)"
    - "All UI text is in Italian across all views and feedback messages"
  artifacts:
    - path: ".planning/phases/04-integration-verification/04-UAT.md"
      provides: "Flow-based UAT checklist with human sign-off results for all 5 success criteria"
      contains: "status: complete"
  key_links:
    - from: "UAT checklist"
      to: "Live Vercel URL"
      via: "Manual human walkthrough on desktop browser + real mobile device"
      pattern: "result: pass"
    - from: "QR scan"
      to: "last_checkin timestamp display"
      via: "Wait for 4-second success animation to clear, then timestamp appears"
      pattern: "Ultimo:"
---

<objective>
Write the flow-based UAT checklist document and pause for human walkthrough on the live Vercel URL.

Purpose: Phase 4 ends with a human signing off that the complete application works end-to-end on the live deployment — all 5 success criteria from ROADMAP.md verified. The UAT document records which tests passed, which had issues, and captures the final result. Regressions discovered during walkthrough must be fixed inline before sign-off.

Output: `.planning/phases/04-integration-verification/04-UAT.md` — a completed checklist with all results recorded, status: complete, signed off by human tester.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-integration-verification/04-CONTEXT.md
@.planning/phases/04-integration-verification/04-RESEARCH.md
@.planning/phases/04-integration-verification/04-01-SUMMARY.md
@.planning/phases/02-ui-component-migration/02-UAT.md

<interfaces>
<!-- Key behavioral contracts for UAT verification. -->

Auth credentials (hardcoded in AuthController.tsx):
  Mock login: email = demo@example.com, password = password123
  Registration: any valid form data creates a new mock user (no credential validation)

Full user flow:
  Register → auto-login to dashboard (no separate login step after register)
  Scan any QR code → success state (green checkmark ~4 seconds) → timestamp appears
  Timestamp display: only visible when user.last_checkin is set AND success animation is NOT showing
  Logout → returns to login view, localStorage cleared

School dropdown behavior:
  RIMINI_SCHOOLS last entry: { value: 'altro', label: 'Altro (Specifica)' }
  Selecting 'altro': custom text input appears below dropdown (animated slide-in)
  All other school selections: no custom input shown
  Submitting with altro: custom text value goes to user profile (not 'altro')

Phase 2 UAT issue (must re-test):
  Test 9 previously failed: NotReadableError: Failed to allocate videosource
  Root cause was called "environmental" (another app holding camera)
  Phase 4 adds user-facing error message — re-test on mobile with all other apps closed
  If live feed appears: Phase 2 issue confirmed environmental, test passes
  If silent hang: the new cameraError state from Plan 01 should now show Italian error text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write flow-based UAT checklist document</name>
  <files>
    .planning/phases/04-integration-verification/04-UAT.md
  </files>
  <action>
    Create `.planning/phases/04-integration-verification/04-UAT.md` with the following
    content structure. Do NOT pre-fill results — leave all result fields as empty strings
    awaiting human tester input.

    The document must be structured as follows:

    **Frontmatter:**
    ```yaml
    ---
    status: in-progress
    phase: 04-integration-verification
    source: [04-01-SUMMARY.md]
    started: [current ISO timestamp]
    updated: [current ISO timestamp]
    vercel_url: "[TO BE CONFIRMED — check Vercel dashboard before starting]"
    ---
    ```

    **Section: Pre-Flight (before browser testing)**
    Note that these were confirmed in Plan 01:
    - `npx next build` passes
    - `npx tsc --noEmit` passes
    - lang="it" set on html element
    - Camera error handling added to QRScanner.tsx

    **Section: Flow 1 — Registration and Auto-Login**
    Tests:
    1. Open the Vercel URL on a desktop browser. The page shows the login form with
       glassmorphism card and mesh gradient background. Italian text visible.
    2. Click "Crea un account" to navigate to the registration form. All 6 fields render:
       Nome, Cognome, Email, Scuola (dropdown), Data di Nascita, Password.
    3. Open the Scuola dropdown. Scroll to the bottom — "Altro (Specifica)" is present.
       Select it. A custom text input appears below the dropdown.
    4. Select a regular school (not Altro) from the dropdown. The custom text input disappears.
    5. Fill all fields with test data. Submit the form. The dashboard view appears
       immediately with the registered user's profile data (name, school, email visible
       in the profile card).

    **Section: Flow 2 — Dashboard and Session Persistence**
    Tests:
    6. On the dashboard, the glassmorphism profile card shows initials avatar, full name,
       school, and email. All text is in Italian labels.
    7. Refresh the browser page (F5 / Cmd+R). The dashboard view remains — the user is
       still logged in. The login form does NOT flash before the dashboard appears.

    **Section: Flow 3 — QR Check-In (desktop simulation)**
    Tests:
    8. Click the camera button on the dashboard. The QR scanner overlay opens — full
       dark overlay with "Inquadra il QR Code..." instruction text.
    9. On desktop: deny camera permission when prompted (if prompted). The scanner
       overlay should show an Italian error message ("Impossibile accedere alla
       fotocamera. Verifica i permessi del browser.") instead of a silent hang.
       NOTE: This tests the Plan 01 camera error fix.
    10. Close the scanner overlay (X button). Return to normal dashboard view.

    **Section: Flow 4 — QR Check-In (mobile — REQUIRED)**
    Tests:
    11. Open the Vercel URL on a REAL MOBILE DEVICE (phone browser, not desktop).
        NOTE: HTTPS is required for camera access — Vercel URL provides this.
        Close all other apps that might hold the camera (Teams, Zoom, camera app, etc.)
        before starting this test.
    12. Navigate to the dashboard (register or login with demo@example.com / password123).
        Click the camera button. The browser requests camera permission. GRANT permission.
        A live camera feed appears inside the scanner overlay.
        NOTE: This is a re-test of Phase 2 UAT test 9 which previously failed with
        NotReadableError. If the live feed appears, the Phase 2 issue was environmental.
        If the error message appears, the cameraError fix is working correctly.
    13. Point the phone camera at any QR code (printed, or displayed on another screen).
        The scanner detects it. The overlay closes and a green checkmark appears on the
        dashboard with Italian success text.
    14. Wait approximately 4 seconds for the green checkmark to dismiss. After it clears,
        a timestamp appears below the camera button showing the check-in time in Italian
        format (e.g., "Ultimo: 14 mar, 10:30").

    **Section: Flow 5 — Logout and Login**
    Tests:
    15. Click the logout button on the dashboard. The login form appears. The dashboard
        is no longer visible.
    16. Enter incorrect credentials (e.g., wrong@email.com / wrongpass). Submit. An
        Italian error message appears: "Credenziali non valide. Usa demo@example.com / password123"
    17. Enter demo@example.com and password123. Submit. The dashboard view appears.
        The last_checkin timestamp from the previous session is still visible (persisted
        in localStorage from the earlier QR scan).

    **Section: Visual Parity**
    Tests:
    18. All views (login, register, dashboard, scanner overlay) show the purple/blue
        animated mesh gradient as the page background.
    19. Form cards and the dashboard profile card appear as frosted glass (translucent,
        slightly blurred, with subtle border) against the gradient.
    20. Form inputs appear as glass-styled inputs (not plain white browser defaults).
    21. The submit/primary buttons use the amber/orange color scheme.

    **Section: Summary**
    Template fields: total, passed, issues, pending, skipped

    **Section: Gaps**
    Empty array by default — filled in by human tester if issues found.

    Commit the UAT checklist file immediately after creating it:
    `docs(04-02): create flow-based UAT checklist for Phase 4`
  </action>
  <verify>
    File exists: `.planning/phases/04-integration-verification/04-UAT.md`
    File contains all 5 flow sections (Registration, Session, QR desktop, QR mobile, Logout/Login).
    File contains at least 20 numbered test items.
    File contains the visual parity section.
    File is committed to git.
  </verify>
  <done>
    04-UAT.md exists with complete flow-based checklist covering all 5 ROADMAP success criteria. All result fields are empty, awaiting human tester input. File committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 applied two inline fixes (lang="it", QrScanner camera error state) and
    confirmed a clean build. This task created the flow-based UAT checklist in
    `.planning/phases/04-integration-verification/04-UAT.md` with 21 tests across
    5 flows covering all ROADMAP success criteria.
  </what-built>
  <how-to-verify>
    1. Find your live Vercel URL (check the Vercel dashboard if unsure).
       Record it in 04-UAT.md frontmatter under `vercel_url`.

    2. Walk through the UAT checklist in `.planning/phases/04-integration-verification/04-UAT.md`
       from top to bottom, against the LIVE Vercel URL. Do NOT use localhost.

    3. For each test, record the result in 04-UAT.md:
       - `result: pass`
       - `result: issue` + `reported: "description"` + `severity: minor|major|blocker`

    4. For the MOBILE test (tests 11-14): use a real phone. Close all other apps first.
       HTTPS is already provided by the Vercel URL — no extra setup needed.

    5. If you find a regression or bug:
       - Record it in 04-UAT.md with result: issue
       - Fix the code inline (it is in scope for this phase to fix regressions)
       - Re-test the fixed item
       - Update the result to pass
       - Commit the fix before proceeding

    6. When all tests are complete, update 04-UAT.md:
       - Set `status: complete`
       - Fill in the Summary section (total, passed, issues counts)
       - Fill in the Gaps section if any unresolved issues remain

    7. Commit the completed UAT document:
       `test(04-02): complete Phase 4 UAT walkthrough`

    Expected outcomes:
    - All 21 tests pass (or minor issues documented and fixed inline)
    - Mobile camera test confirms Phase 2 environmental issue OR confirms cameraError fix works
    - App is shippable for v1
  </how-to-verify>
  <resume-signal>
    Type "approved" when all UAT tests are complete and 04-UAT.md is updated with results.
    Or describe any blocking issues found that could not be fixed inline.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:

1. 04-UAT.md exists with status: complete
2. All ROADMAP success criteria verified:
   - Full flow: register → auto-login → dashboard → scan QR → timestamp → logout → login again
   - Glassmorphism styles present on all views
   - School dropdown Altro shows/hides custom input correctly
   - Camera works on mobile (or shows Italian error message if denied)
   - Italian text on all forms and feedback messages
3. Build still passes (npx next build exits 0)
4. Phase 4 summary created
</verification>

<success_criteria>
- 04-UAT.md exists with status: complete and all test results recorded
- All 5 ROADMAP success criteria verified (pass or documented + fixed)
- Mobile camera re-test closes the Phase 2 UAT issue (test 9 either passes or cameraError message confirms fix)
- App is shippable: complete user flow works end-to-end on live Vercel URL
- v1 migration from React+Vite to Next.js is complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-verification/04-02-SUMMARY.md`
</output>
